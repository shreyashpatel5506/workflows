name: Keep Render Alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Wake Render Apps
        run: |
          curl -s https://virtualassistant-wxxq.onrender.com > /dev/null
          curl -s https://pulsetalk-6lrk.onrender.com/ > /dev/null
          curl -s https://ecommersebackendshreyash.onrender.com/ > /dev/null